import React from 'react';
import './styles.css';

const Spinner = props => {
  return (
    <div className="loader">
      <div className="loader__spinner" />
    </div>
  );
};

export default Spinner;
